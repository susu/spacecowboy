include_rules

LIBS+=-lscphi -lscevt
EXTRADEP+=../libscphi.so ../../evt/libscevt.so

: test_*.hpp |> !generate_test |> testFile.cpp
: foreach *.cpp |> !cpp |>
: testFile.o |> !link_test |>
: |> !run_test |>

