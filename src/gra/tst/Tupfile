include_rules

LIBS+=-lscphi -lscevt -lscgra
EXTRADEP+=../libscgra.so ../../phi/libscphi.so ../../evt/libscevt.so

: test_*.hpp |> !generate_test |> testFile.cpp
: foreach *.cpp |> !cpp |>
: testFile.o |> !link_test |>
: |> !run_test |>

